#!/usr/bin/env bash

set -o errexit

[[ ${ERLANG_VERSION} ]] || {
  echo 'Missing $ERLANG_VERSION' >&2
  exit 1
}

kerl update releases
rm -rf "${HOME}/otp/${ERLANG_VERSION}"
kerl build "${ERLANG_VERSION}" "${ERLANG_VERSION}"
kerl install "${ERLANG_VERSION}" "${HOME}/otp/${ERLANG_VERSION}"
